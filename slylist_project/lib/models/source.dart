class Source {
  String id;
  String name;
  bool isSelected;
  bool isDefault;

  Source(String id, String name, bool isSelected, bool isDefault) {
    this.id = id;
    this.name = name;
    this.isSelected = isSelected;
    this.isDefault = isDefault;
  }
}
